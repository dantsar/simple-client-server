#include <getopt.h>
#include <stdio.h>

#include <server/server.h>

int main(int argc, char** argv)
{
    /* parse args */
    // static struct option long_options[] = {
    //     {"hostname",    required_argument, NULL, 'H'},
    // };

    fprintf(stdout, "Hello there howdy there partner!!\n");

    return 1;
}
